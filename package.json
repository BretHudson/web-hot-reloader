{
	"name": "web-hot-loader",
	"version": "0.3.1",
	"description": "A CSS hot loader for the browser",
	"main": "app/index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"type": "module",
	"author": "Bret Hudson",
	"license": "ISC",
	"dependencies": {
		"socket.io": "2.3.0"
	}
}
